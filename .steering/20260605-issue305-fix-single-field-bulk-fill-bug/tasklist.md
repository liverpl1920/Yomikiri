# Issue #305 タスクリスト

## タスク一覧

- [ ] `_fetchBookByTitle` に `fillForm` オプションを追加
- [ ] `_performFetchByTitle` に `fillForm` オプションを追加し、`fillForm: false` の場合はフォーム入力をスキップ
- [ ] `fetchSingleField` の呼び出しを `{ fillForm: false }` に変更
- [ ] バグ再現テスト（キャッシュなし状態での個別補完が他項目を上書きしないこと）を追加
- [ ] `bundle exec rspec` で検証
- [ ] `bundle exec rubocop` で検証（JSは対象外）
- [ ] コミット & プッシュ & PR作成

## 振り返り
（作業完了後に記載）
