# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: ロケール定義の作成と修正

- [x] 多言語化定義の修正
  - [x] `config/locales/ja.yml` に `streak.unit` (zero/other) を定義
  - [x] `config/locales/en.yml` に `streak.unit` (zero/one/other) を定義

## フェーズ2: ビューとスタイルの実装

- [x] ダッシュボードの表示改善
  - [x] `app/views/dashboards/show.html.erb` でストリークの数値と単位を分割表示
  - [x] `app/assets/stylesheets/dashboards.css` に `.dashboard__stat-unit` スタイルを追加
- [x] マイページの表示改善
  - [x] `app/views/mypages/show.html.erb` で連続読書日数の数値と単位を分割表示
  - [x] `app/assets/stylesheets/mypages.css` に `.mypage__stat-unit` スタイルを追加

## フェーズ3: 品質チェックと修正

- [x] すべてのテストが通ることを確認
  - [x] `bundle exec rspec`
- [x] リントエラーがないことを確認
  - [x] `bundle exec rubocop`

## フェーズ4: ドキュメント更新と振り返り

- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-08-02

### 計画と実績の差分

**計画と異なった点**:
- なし

**新たに必要になったタスク**:
- 読書ストリークが `0` の場合と `1` 以上の非ゼロの場合における表示切り替えテストケースの System Spec への追加

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- なし

### 学んだこと

**技術的な学び**:
- RailsのI18nにおいて、pluralization用の `zero` および `other` キーを定義して `t('key', count: value)` と呼び出すことで、ビュー側の条件分岐を減らしつつ値に基づく単位切り替えをエレガントに実装できることを学びました。

**プロセス上の改善点**:
- テストケースで `within` を使って特定のカード内の表示をスコープすることで、変更箇所（統計数値と新設の単位タグ）のテストをより正確に行うことができました。

### 次回への改善提案
- 特になし。今後も同様にI18nの強力なロケール分岐を活用したい。
