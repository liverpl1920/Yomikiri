# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

---

## フェーズ1: 準備と実装

- [x] Gitブランチの作成
  - `git checkout main`
  - `git pull origin main`
  - `git checkout -b feature/#297-rating-reverse-bug`

- [x] 評価更新フォームの修正
  - `app/views/books/show.html.erb` の L446 の星ループを `(1..5).reverse_each` に変更

## フェーズ2: テストと検証

- [x] 既存テストの実行
  - `bundle exec rspec spec/system/books/review_spec.rb` がパスすることを確認する
- [x] テストの補強（HTML構造の順序テスト追加）
  - `spec/system/books/review_spec.rb` に、詳細画面の評価更新フォームのラジオボタンが降順（valueが5, 4, 3, 2, 1）で並んでいることを検証するテストを追加する
- [x] RuboCop の実行と修正
  - `bundle exec rubocop` で対象ファイルをチェックする

## フェーズ3: コミットとPR作成

- [ ] コミット
  - `git add .`
  - `git commit -m "#297 Fix rating star selection value inversion"`
- [ ] プッシュ & PR作成
  - `git push origin feature/#297-rating-reverse-bug`
  - `gh pr create --title "#297 詳細画面の評価（★）の更新で星の選択値が反転して保存される不具合の修正" --body "詳細画面での評価更新フォームにおいて、HTMLの出力順序とCSSのrow-reverseの不整合により、評価値が反転して保存される問題を修正しました。" --base main --head feature/#297-rating-reverse-bug`

## フェーズ4: 振り返り

- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}

**新たに必要になったタスク**:
- {実装中に追加したタスク}

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- {タスク名}

### 学んだこと

**技術的な学び**:
- {実装を通じて学んだ技術的な知見}

**プロセス上の改善点**:
- {タスク管理で良かった点}

### 次回への改善提案
- {次回の機能追加で気をつけること}
