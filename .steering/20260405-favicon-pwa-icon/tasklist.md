# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

---

## フェーズ1: アイコンファイルの差し替え

- [x] `public/icon.svg` を Yomikiri ブランドデザインに差し替える
  - [x] 青背景（#2563eb）角丸SVGに白い「Y」テキストを描画
  - [x] 既存の `<rect fill="red"/>` を削除

- [x] `public/icon.png` を新デザインに差し替える
  - [x] 512×512 の PNG を生成（SVGから変換）
  - [x] 既存の赤い四角プレースホルダーを置き換え

## フェーズ2: PWAマニフェストの修正

- [x] `app/views/pwa/manifest.json.erb` を修正する
  - [x] `theme_color` を `"#2563eb"` に変更
  - [x] `background_color` を `"#ffffff"` に変更
  - [x] `description` を `"積読を消化する技術 — 読了期限とノルマで読書を行動に変えるアプリ"` に変更

## フェーズ3: 品質チェック

- [x] 既存のテストが全て通ることを確認
  - [x] `bundle exec rspec`
- [x] RuboCop エラーがないことを確認
  - [x] `bundle exec rubocop`

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}

### 学んだこと

- {今回の実装で得た知見}

### 次回への改善提案

- {次回以降に活かせること}
