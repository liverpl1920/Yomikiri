# マイページ ジャンル設定の不具合修正およびレイアウト調整 - タスクリスト

## TODO

### 1. 調査とシステムテストの追加
- [x] `spec/system/mypages/mypage_spec.rb` にジャンル設定の表示、追加、編集、削除に関するテストケースを追加する。
- [x] テストを実行し、既存のジャンルが「存在していないことになっている」現象の原因を特定する。

### 2. ジャンル設定不具合の修正
- [x] 特定された原因に基づいてコード（コントローラ、ビュー、アソシエーション等）を修正する。
- [x] テストを実行し、不具合が修正されたことを確認する。

### 3. レイアウトの調整
- [x] `app/views/mypages/show.html.erb` の「ジャンル設定」および「アカウント設定」カードの配置位置を「読書実績」の上に移動する。
- [x] 移動後の表示が崩れていないか、テストや目視等で確認する。

### 4. 最終検証
- [x] `bundle exec rspec` を実行し、すべてのテストがパスすることを確認する。
- [x] `bundle exec rubocop` を実行し、コード規約に違反がないことを確認する。
- [x] 完了報告を作成する。
