# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: バグ修正実装

- [x] コントローラーの修正
  - [x] `app/controllers/dashboards_controller.rb` に今年読了した本のカウント値 `@this_year_books_read` を定義する。
- [x] ビューの修正
  - [x] `app/views/dashboards/show.html.erb` の「年間目標」の箇所で、分子として `@total_books_read` ではなく `@this_year_books_read` を参照する。

## フェーズ2: テストの作成と実行

- [x] テストの作成・修正
  - [x] `spec/system/dashboards_spec.rb` を確認し、今年読了した本と前年以前に読了した本が正しく分類され、年間目標の分子には今年のもののみがカウントされ、読書統計の累計には両方がカウントされることを検証するテストを追加する。
- [x] テストの実行とパス確認
  - [x] `bundle exec rspec` を実行し、すべてのテストが通ることを確認。

## フェーズ3: 品質チェックと修正

- [x] 静的解析（リント）の確認
  - [x] `bundle exec rubocop` を実行し、コードの書き方が規約に適合していることを確認。

## フェーズ4: 振り返り

- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-06-07

### 計画と実績の差分

**計画と異なった点**:
- 特になし。

**新たに必要になったタスク**:
- 特になし。

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- なし

### 学んだこと

**技術的な学び**:
- ActiveSupportの `Time.current.all_year` を用いて、特定の年の読了データを正確かつ簡潔に抽出する方法を再確認した。また、累計統計値と年間目標値で異なるカウント処理が必要であるという仕様上の注意点を考慮した設計の大切さを学んだ。

**プロセス上の改善点**:
- 計画時に影響箇所とテストシナリオを正確に設計できたため、実装とテスト追加を極めてスムーズに進めることができた。

### 次回への改善提案
- コントローラー内で `Time.current.all_year` のような日付の絞り込みを行う際、将来的にクエリが増えた場合はModelのscopeなどに切り出すことも検討すると良い。
