# Tasklist: スマホ画面でのボタンサイズ・レイアウトバランス改善 (#195)

## フェーズ1: header_footer.css のモバイル対応

- [x] ゲストヘッダーボタン（430px以下）のサイズ・余白調整を追加
- [x] 極小モバイル（360px以下）向けヘッダーギャップ最小化を追加

## フェーズ2: top.css のモバイル対応修正

- [x] `top-header__nav` 参照を `site-header__nav` に修正（または削除）
- [x] 既存480px以下メディアクエリにヒーローCTA `btn--lg` の幅指定追加
- [x] 430px以下のヒーローCTAボタンサイズ調整を追加

## フェーズ3: auth.css のモバイル対応

- [x] 認証カードのモバイル向けpadding調整を追加
- [x] `.btn--full` のタップ領域・フォントサイズ調整を追加

## フェーズ4: 動作確認

- [x] RuboCop実行（CSSは対象外だがRubyコード変更なし確認）
- [x] RSpec実行（既存テストの回帰確認）

## 振り返り

### 実装完了日
2026-05-23

### 計画と実績の差分
- 計画通りCSSのみの変更で対応完了。HTMLの変更は不要だった
- `top.css` に存在した `top-header__nav` 参照（実際には使用されていないクラスへの参照）を削除し、代わりにヒーローCTAボタンの幅指定を追加した
- `auth.css` にモバイル向けpadding/タップ領域の調整を追加

### 変更サマリー
| ファイル | 変更内容 |
|---|---|
| `header_footer.css` | 430px/360px以下でヘッダーボタンのサイズ縮小、ロゴ縮小 |
| `top.css` | 不要な `top-header__nav` 参照を削除、480px以下でCTAボタン全幅化 |
| `auth.css` | 430px以下で認証カードのpadding縮小、`.btn--full` のタップ領域確保 |

### 学んだこと
- `top.css` に `top-header__nav` という使われていないセレクタが残っていた（共通ヘッダーは `site-header__nav` を使用）
- モバイル対応はブレークポイント戦略を統一することで管理しやすくなる（430px基準）
- RuboCop・RSpecは全て通過し（486例）、CSS変更による既存機能への影響なし

### 次回への改善提案
- 今後モバイル対応を追加する際は `header_footer.css` の `@media (max-width: 430px)` ブロックに集約する
- テスト戦略としてシステムスペックでViewportを切り替えたモバイルUIテストの追加を検討
