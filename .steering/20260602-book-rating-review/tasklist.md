# タスクリスト (TaskList)

## 1. 準備・調査

- [x] ISSUE内容の確認
- [x] 既存の読了フローとUIの調査
- [x] 実装用のブランチを作成する

## 2. データベース拡張

- [x] `rating` 列と `review` 列を追加するマイグレーションを作成する（既存であったことを確認）
- [x] マイグレーションを実行する（実行済み）
- [x] `Book` モデルにバリデーションを追加する (rating: 1-5, review: max length)

## 3. Web API / コントローラ

- [x] `BooksController#update_review` アクションを定義する（既存であったことを確認）
- [x] `routes.rb` にパスを追加する（既存であったことを確認）
- [x] `book_params` に新カラム（rating, review）を追加する

## 4. UI実装（読了お祝いモーダル）

- [x] 星評価（★）のCSSスタイルを作成する
- [x] `books/show.html.erb` の `celebration-modal` 内に評価・感想入力フォームを実装する
- [x] 保存ボタンを追加し、コントローラと連携させる

## 5. UI実装（詳細画面での表示）

- [x] `books/show.html.erb` で読了済みの書籍に評価と感想を表示するように変更する
- [x] スタイルを微調整する

## 6. テスト・検証

- [x] 評価・感想が正常に保存されることを確認するシステムテストを書く
- [x] 既存の読了フローが壊れていないことを確認する
- [ ] Rubocopによるコードチェック
- [x] RSpecによる全テスト実行（一部既存の不安定なテストを除きパスを確認）

## 7. 完了

- [ ] PRを作成する
- [ ] 振り返りを記載する
