# タスクリスト

## 🚨 タスク完全完了の原則

このファイルの全タスクが完了するまで作業を継続すること。

---

## フェーズ1: 一覧画面の表示改修

- [x] `books#index` 一覧カードに読了本評価表示を追加
	- [x] `app/views/books/index.html.erb` に `book.completed? && book.rating.present?` の条件表示を追加
	- [x] `rating` の値に応じた `★` 表示（1-5）を実装
	- [x] 評価未設定・未読/読書中で表示されないことを確認

## フェーズ2: テスト追加・更新

- [x] 一覧評価表示に関するRequest specを追加/更新
	- [x] 読了済み・評価ありの表示検証
	- [x] 読了済み・評価なしの非表示検証
	- [x] 未読/読書中の非表示検証

## フェーズ3: 品質チェック

- [x] RSpec を実行し、回帰がないことを確認
	- [x] `bundle exec rspec spec/requests/books_spec.rb spec/requests/books_index_spec.rb`
- [x] RuboCop を実行し、警告/エラーがないことを確認
	- [x] `bundle exec rubocop spec/requests/books_spec.rb spec/requests/books_index_spec.rb`

## フェーズ4: ドキュメントと振り返り

- [x] 影響範囲を確認し、永続ドキュメント更新要否を判断
- [x] 実装後の振り返りを本ファイル下部に記載

---

## 実装後の振り返り

### 実装完了日
2026-05-27

### 計画と実績の差分
- 主要実装（一覧への評価表示、表示条件分岐、Request spec追加）は計画通りに完了。
- 実装検証で判明した改善点として、評価境界値（1/5）のテストを追加した。
- RuboCop 実行コマンドは Ruby ファイル対象に修正し、実行可能な品質チェックに合わせた。

### 学んだこと
- 一覧表示の条件分岐はビューで閉じることで、モデル変更なしに要件追加を安全に反映できる。
- Request spec でカード単位にDOM確認すると、表示条件の回帰を明確に検出しやすい。

### 次回への改善提案
- 評価表示をヘルパー化して、詳細画面と一覧画面の表現差分をさらに減らす。
- ERB/CSSの静的検査を導入する場合は、RuboCopとは別ツールを明示的に運用する。
