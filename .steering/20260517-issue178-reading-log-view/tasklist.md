# Issue #178 Task List

## タスク一覧

- [x] 既存実装を調査し、進捗更新とマイページ表示の変更ポイントを特定する
- [x] `reading_logs` テーブル作成 migration と `ReadingLog` モデルを実装する
- [x] `BooksController#update_progress` で読書ログを記録する処理を実装する
- [x] マイページに日別読書ログ（本と読了ページ数）表示を実装する
- [x] model/request/system のテストを追加する
- [x] RSpec を実行し、変更点の動作を検証する
- [x] RuboCop を実行し、静的解析エラーがないことを確認する

## 振り返り

### 実装完了日
2026年5月17日

### 計画と実績の差分
- 計画通り、読書ログの永続化と可視化を同時に実装した
- 追加で `direct_page` 更新時の増分のみ記録ルールを明文化して実装した
- PR レビュー指摘対応として、直近7日固定表示・ゼロ件日の明示・曖昧入力時の記録整合性を追加実装した
- CI（bundler-audit）失敗対応として依存 gem をセキュア版に更新した

### 学んだこと
- 進捗更新を「現在ページの値」だけで扱うと履歴分析が難しいため、イベントログとして別テーブル化するほうが拡張性が高い
- マイページは既存カード構成が整っていたため、同じ BEM パターンで追加することで UI 一貫性を維持できた

### 次回への改善提案
- 日別読書ログにページ合計（日次合計）を表示すると、週次・月次レポート機能への接続が容易になる
