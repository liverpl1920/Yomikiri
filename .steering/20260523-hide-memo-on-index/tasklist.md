# タスクリスト: 一覧ページでメモ欄を表示しないようにする

## フェーズ1: 準備

- [x] mainブランチから feature/#215-hide-memo-on-index ブランチを作成

## フェーズ2: 実装

- [x] app/views/books/index.html.erb からメモ表示ブロックを削除

## フェーズ3: テスト・検証

- [x] bundle exec rspec を実行して全テスト通過を確認
- [x] bundle exec rubocop を実行してエラーなしを確認

## フェーズ4: コミット・PR

- [x] 変更をコミット（#215 一覧ページでメモ欄を非表示にする）
- [x] リモートにプッシュしてPRを作成
- [x] CI確認

---

## 実装後の振り返り

**実装完了日**: 2026-05-23

**計画と実績の差分**:
- 計画通り。変更は `index.html.erb` の3行削除のみで完結した。
- 既存テスト (`books_index_spec.rb`) でメモ表示を期待している1件を、非表示を検証する内容に更新した。

**学んだこと**:
- 既存のspecが「表示されること」を検証していた場合、仕様変更に合わせてspecも更新する必要がある。
- 単純な非表示変更であっても spec の更新を忘れないこと。

**次回への改善提案**:
- 特になし。変更内容が小さく、影響範囲も明確だった。
