# タスクリスト

## 🚨 タスク完全完了 of 原則

**このファイルの全タスクが完了するまで作業を継続すること**

## フェーズ1: スタイル定義

- [x] 1. `app/assets/stylesheets/books.css` にスタイルを追加する
  - [x] 非表示用のモディファイアクラス `.books-index__search--hidden` を定義
  - [x] ヘッダーアクション領域用のクラス `.books-index__header-actions` を定義
  - [x] レスポンシブ表示用のスタイルを定義

## フェーズ2: Stimulus コントローラの作成

- [x] 2. `app/javascript/controllers/search_toggle_controller.js` を作成する
  - [x] form ターゲット、button ターゲットを定義
  - [x] active 値（Boolean）を定義
  - [x] active 値の変化に応じてフォームの表示/非表示クラスを切り替えるロジックを実装
  - [x] ボタンの `aria-expanded` 属性を同期するロジックを実装

## フェーズ3: ビューの修正

- [x] 3. `app/views/books/index.html.erb` を修正する
  - [x] `search-toggle` コントローラをバインド
  - [x] ヘッダーに `.books-index__header-actions` を追加
  - [x] トグルボタン「検索」を追加
  - [x] 検索フォームに id, targets, actions, aria-hidden などの属性を追加

## フェーズ4: システムテストと品質検証

- [x] 4. システムテスト `spec/system/books/search_toggle_spec.rb` を追加する
  - [x] 初期状態で検索条件がなければ非表示であるテスト
  - [x] ボタン押下でフォームが表示・非表示されるテスト
  - [x] 初期状態で検索条件があれば表示されているテスト
- [x] 5. テストと静的解析を実行し、すべて通過することを確認する
  - [x] `bundle exec rspec`
  - [x] `bundle exec rubocop`

## フェーズ5: コミット & PR作成

- [ ] 6. 変更をコミットし、GitHubにプッシュしてPRを作成する
  - [ ] コミットメッセージに #281 を含める
  - [ ] `gh pr create` でPRを作成する

## フェーズ6: 振り返り

- [ ] 7. タスクリストの振り返りセクションを更新する

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}

**新たに必要になったタスク**:
- {実装中に追加したタスク}

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- {タスク名}

### 学んだこと

**技術的な学び**:
- {実装を通じて学んだ技術的な知見}

**プロセス上の改善点**:
- {タスク管理で良かった点}

### 次回への改善提案
- {次回の機能追加で気をつけること}
