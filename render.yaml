services:
  - type: web
    name: yomikiri
    runtime: ruby
    region: oregon
    plan: free
    buildCommand: |
      bundle install &&
      RAILS_MASTER_KEY=00000000000000000000000000000000 SECRET_KEY_BASE_DUMMY=1 bundle exec rails assets:precompile &&
      bundle exec rails assets:clean
    startCommand: bundle exec rails db:migrate && bundle exec puma -C config/puma.rb
    healthCheckPath: /up
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: RAILS_SERVE_STATIC_FILES
        value: "true"
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
